interface:
  display_name: "Build Review Loop"
  short_description: "Run the public blinded review-loop pilot"
  default_prompt: "Use $build-review-loop to run the public protocol-v2 neutral-build review-loop experiment."
