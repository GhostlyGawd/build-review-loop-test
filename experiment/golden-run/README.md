# Prospective conformance fixture

`golden-run.json` is synthetic protocol evidence used only to test execution-mode validation. Its identifiers and snapshots are deterministically derived test values, not candidate output, hidden-suite input, or experiment evidence. It must never be interpreted as an executed experiment.
