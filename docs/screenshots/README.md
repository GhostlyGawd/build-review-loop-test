# Screenshot evidence

Both PNG files are real, sanitized captures of the local Vite production preview. They contain only the frozen fictional fixture and no personal data, customer data, credentials, private paths, or third-party content.

| File                | Alt text / caption                                                                                                         | Capture date | Viewport           | Source                                                                                                            |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------ | ------------------ | ----------------------------------------------------------------------------------------------------------------- |
| `explicit-deny.png` | Check access shows Ben denied Edit access to Runbook by explicit `rule-4`, above the effective-permissions matrix.         | 2026-07-20   | 1440 × 1000 CSS px | Local Chromium capture from `npm run build` + `vite preview`; revision `cd66ab1c9f76eeb72167e58722be96d199ecb635` |
| `allowed.png`       | Check access shows Cy allowed View access to Budget by the more-specific `rule-5`, above the effective-permissions matrix. | 2026-07-20   | 1440 × 1000 CSS px | Local Chromium capture from `npm run build` + `vite preview`; revision `cd66ab1c9f76eeb72167e58722be96d199ecb635` |

The static PNG files are the fallback evidence. They were generated from original project UI; no external or AI-generated visual assets are present.
